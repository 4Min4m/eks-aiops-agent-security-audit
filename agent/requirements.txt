langgraph==0.2.45
langchain-core==0.3.15
langchain-ollama==0.2.0
fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
kubernetes==31.0.0
pydantic==2.9.2
python-dotenv==1.0.1
# agento11y is Grafana's renamed Agent Observability SDK (formerly
# `sigil-sdk`) — see docs/INTERVIEW-PREP-aiops-agent.md and the top-level
# README's "Naming note" section. Pin whatever the current published
# version is when you actually run this; placeholder pin here since the
# rename happened after this golden copy was written offline.
agento11y==0.3.0
